 void moveZeroes(vector<int>& nums) {
        int n=0,nz=0;
        while(n<nums.size() && nz<nums.size()){
            if(nums[nz]!=0)
                swap(nums[nz++],nums[n++]);
            else
                nz++;
                
        }
        
        
    }
