void selectionSort(int arr[], int n)
    {
       //code here
       int min=0;
       for(int i=0;i<n;i++){
           min = i;
           for(int j=i+1;j<n;j++){
               if(arr[j]<arr[min]){
                   swap(arr[i],arr[j]);
               }
           }
       }
    }
